
## docker 常见命令

### docker 
```
docker ps
docker build -t <name> .
docker image ls
docker run -p <docker port:nginx port> <name>
```

### docker compose
```
docker-compose ps
docker-compose up -d
docker-compose down
```